{
  "name": "mosaic-shell",
  "version": "0.0.1",
  "private": true,
  "description": "The Mosaic Shell — a server-driven-UI web client for the Mosaic media platform.",
  "license": "AGPL-3.0-only",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:pkg": "npm run build -w @mosaic-media/sdui-react",
    "dev": "npm run build:pkg && vite",
    "build": "npm run build:pkg && tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "npm run build:pkg && tsc -b --noEmit"
  },
  "dependencies": {
    "@mosaic-media/sdui-react": "*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
